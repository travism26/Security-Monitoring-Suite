package main

import "fmt"

// Lets keep it simple for now and just print a message
// next lets pull some messages from the kafka queue
// and then we can store them in a database
// and then we can serve them up via a rest api
func main() {
	fmt.Println("Starting Log Aggregator Server...")
}
